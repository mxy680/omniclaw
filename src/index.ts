import { register } from "./plugin";

const omniclawPlugin = {
  id: "omniclaw",
  name: "omniclaw",
  description: "Gmail integration for OpenClaw via Google OAuth2",
  version: "0.1.0",
  register,
};

export default omniclawPlugin;
